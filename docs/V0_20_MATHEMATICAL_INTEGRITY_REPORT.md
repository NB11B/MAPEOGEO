# MAPEOGEO v0.20 Mathematical Integrity Amendment

This is the current integrity report for active reconstruction. Historical reports and sealed evidence remain historical records; their former headlines are not active mathematical claims.

## Source counts

The sealed v0.11 graph has SHA-256 `409a648d8563bc4a027dc3dc29fc53722d11bc489462a0f3d1f71bd4f272544d` and contains exactly 1,356 `STATEMENT` declarations. Active downstream totals are therefore:

| Stage | Active source declarations |
|---|---:|
| v0.16 | 1,756 |
| v0.17 | 1,820 |
| v0.18 | 1,887 |
| v0.19 | 1,959 |
| v0.19 + foundation | 2,135 |

The historical 1,360-source headline is retained only in superseded reports and in the source-count amendment.
The active foundation dashboard names the accounting boundary explicitly: 2,144 raw
registered source records consist of 2,135 admissible declarations, five segregated
section anchors, and four exact historical synthetic Gallier chapter records excluded
by the source-bound amendment. The excluded records remain visible in the graph; they
are not silently counted as source declarations.

## Foundation evidence and grounding

| Channel | Current admissible result |
|---|---:|
| Curated foundation declarations | 176 |
| Executable declaration evidence | 1 / 176 |
| Kernel-verified foundation declarations | 0 / 176 |
| Proof-eligible grounding | 0 / 235 advanced canonical objects |

The sole declaration-level executable certificate is the exhaustive four-row Boolean check for the named De Morgan declaration. Helper checks and representative numerical experiments are not promoted to evidence for other declarations. No foundation declaration is currently kernel verified.

The dashboard separates **raw topology reachability** from **proof-eligible grounding**. Raw topology describes paths in the declared graph. It is not proof. Proof-eligible grounding requires an explicitly validated source root and every traversed edge to match a closed, endpoint-bound evidence registry. Because the active graph currently has no such registered edge-certificate chain, the honest proof-eligible result is 0 / 235, irrespective of raw reachability.

## Active v0.20 alignment projection

The sealed v0.19 alignment file is validated by SHA-256 before use and is never rewritten by the active generator. Amendments are applied in memory:

- the false Axler Definition 5.8 Liouville/FTA alignment is preserved as a wound and excluded from active emission;
- the duplicate Axler Definition 6.55 orthogonal-projection alignment is preserved as a wound and deduplicated in the active view;
- 33 relation conflicts are retained with all contextual originals;
- each conflicting endpoint pair is emitted using the conservative global meet `RELATED_TO < SCOPED_OVERLAP < SAME_SEMANTICS`.

The active projection therefore records 35 alignment wounds: one false alignment, one duplicate, and 33 relation conflicts. It does not mutate sealed v0.19 history.

## Corrected active source statements

The source-bound amendment registry records old and new statement hashes for Cauchy higher derivatives, Schwarz equality, Harnack's principle, Weierstrass zero sets, complex differentiability versus holomorphy, the principal logarithm range, Cauchy's estimates, Montel's theorem, the argument principle, domains of holomorphy, and the Levi boundary criterion. It also binds the Dolbeault reference correction to the exact subject and target statement hashes.

These amendments govern v0.20 active reconstruction only. They do not retroactively alter sealed artifacts or historical evidence.

## Foundation statement amendment

The active foundation projection now applies the separately hash-bound corrections documented in [`FOUNDATION_MATHEMATICAL_AMENDMENT_V0_20.md`](FOUNDATION_MATHEMATICAL_AMENDMENT_V0_20.md). Its registry contains all 176 historical row identities and exactly 84 changed identities, and fails closed on omissions, false listings, or identity drift.

Structural references are emitted only as unverified `STRUCTURAL_REFERENCE` candidates. They are not proof dependencies. A `PROOF_DEPENDENCY` can enter the proof-eligible channel only through closed endpoint-bound edge evidence. The correction registry is not proof evidence, so the active evidence totals remain 1 / 176 executable and 0 / 176 kernel verified.

## Reproducible provenance boundary

The machine-readable report binds the exact raw bytes of the complex and
foundation amendment registry files.  It separately binds domain-separated
canonical digests of the 176-row active foundation declaration registry and
the closed executable contract registry.  A producer source tree digest covers
the Python reconstruction scripts, Lean sources, toolchain declaration, and
Lake manifest that produce or check this evidence.

This content-addressed boundary deliberately does not claim its own Git commit.
A report cannot truthfully contain the identity of the commit that first
contains that report without creating a self-reference.  Cross-branch consumers
must bind the final repair commit externally and verify these raw content hashes.
